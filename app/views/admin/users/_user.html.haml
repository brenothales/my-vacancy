%td= @user.name
%td.actions
  = link_to "<i class='icon-pencil'></i>".html_safe, edit_admin_user_path(@user), :rel => 'tooltip', :title => t('cruds.table_list.tooltips.edit'), :data => { :placement => :top }
  = link_to "<i class='icon-zoom-in'></i>".html_safe, admin_user_path(@user), :rel => 'tooltip', :title => t('cruds.table_list.tooltips.show'), :data => { :placement => :top }
  = show_active_desactive(@user)
